/**
 * 计算反向映射标准词典
 * @param standard
 */
export function reverseStandard(standard) {
    var reversed = {};
    for (var key in standard) {
        reversed[standard[key]] = key;
    }
    return reversed;
}
//# sourceMappingURL=helper.js.map